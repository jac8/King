do

function run(msg, matches)
  return [[crector is
  parsa ghafoori.s
  iKD Comapany
  =================
  @parsaghafoori
  @parsa_masagerbot
  @zatabot
  @anti_spam_group]

end

return {
  description = " bot cerector
  usage = "crector
  patterns = {
    "^[!/#]cerector$"
  }, 
  run = run 
}

end
